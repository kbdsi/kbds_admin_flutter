import 'package:flutter/material.dart';

class ParameterDetailScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('ParameterDetail Screen'),
    );
  }
}
