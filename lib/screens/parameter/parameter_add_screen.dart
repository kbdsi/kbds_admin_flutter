import 'package:flutter/material.dart';

class ParameterAddScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('ParameterAdd Screen'),
    );
  }
}
