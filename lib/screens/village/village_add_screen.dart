import 'package:flutter/material.dart';

class VillageAddScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('VillageAdd Screen'),
    );
  }
}
