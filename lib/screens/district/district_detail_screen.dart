import 'package:flutter/material.dart';

class DistrictDetailScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('DistrictDetail Screen'),
    );
  }
}
