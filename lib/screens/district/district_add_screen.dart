import 'package:flutter/material.dart';

class DistrictAddScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('DistrictAdd Screen'),
    );
  }
}
