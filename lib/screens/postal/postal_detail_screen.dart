import 'package:flutter/material.dart';

class PostalDetailScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('PostalDetail Screen'),
    );
  }
}
