import 'package:flutter/material.dart';

class PostalAddScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('PostalAdd Screen'),
    );
  }
}
